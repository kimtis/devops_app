{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "install": "lerna bootstrap --no-ci",
    "start": "lerna exec -- npm start",
    "clean": "lerna exec -- npm run clean && rm -rf node_modules"
  },
  "dependencies": {}
}
